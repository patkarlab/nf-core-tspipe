# HANDOFF for 2026-09-10 (v2) — nf-core-tspipe, written 2026-09-09 ~21:45

Supersedes v1 §2 (MoChA-17p) and §7 (order). Everything else in v1 (docs/audit/2026-09-09/
HANDOFF_2026-09-10_v1.md) stands: §0 state, §3 open decisions, §4 handout register A1–A10 / B / C,
§5 loose ends, §6 documentation debt.

## 0. State (delta since v1)

- v1 §1 done: GENOME_V2 render confirmed (18/432, 18:44); memo 17 + handoff v1 committed as
  dc05523; 36 `.bak_*` purged. Still unchecked: the 26CGH1043 Genome-wide figure by eye.
- MoChA-17p tested and **parked** — memo 18 (`docs/memos/memo18_mocha17p_feasibility.md`).
  Committed with it: `containers/mocha/`, `tools/patches/2026-09-09/{build_mocha_v1.sh,
  fetch_1kg_chr17_parallel.sh,mocha17_offline.sh}`, `bin/baf_genotypes_vcf.py` (v1.3),
  `assets/twist_myeloid/snp_sites.baf.chr17.panel_alleles.tsv`, outputs under
  `docs/audit/2026-09-09/mocha17p/`. Off-repo: `/goast/hemat_data/references/1000G_hg38_phased/`
  (panel, .tbi, chr17.b38.gmap.gz), `singularity_cache/local-mocha-v1.img`, scratch `/tmp/mocha17/`.
- Run8 untouched since GENOME_V2 (session abea2914; resume line and nocache overlay in v1 §0).
  Nothing running. `bin/baf_genotypes_vcf.py` is new in `bin/` and referenced by no module, so it
  does not touch any task hash.

## 1. First work item: TP53 observation block (dashboard, ~2 h)

Goal: one card that puts the TP53 variant and the 17p allelic evidence side by side as
observations, with the interpretation line governed by Nikhil's rules (WHO-HAEM5 / ICC wording is
his; the block never computes "multi-hit" on its own).

Fields (all already produced by the pipeline; the work is the join and the rendering):
- TP53 variants: every reportable TP53 row of the clinical table — HGVS (VV → CAVA → VEP
  preference as on the Reporting page), VAF, callers, tier, Variant_Class; more than one row
  is itself an observation.
- 17p: consensus row(s) for TP53 in `cnv_consensus4.genes.tsv` — call, cn/log2, tier, arm
  letters, allelic_state; BAF_V2 arm row for 17p from `clinical/cnv/baf/<S>.baf.summary.tsv` —
  verdict, f, n het sites, confidence, scope; PURPLE total/minor CN and purity at TP53;
  PureCN LOH flag if present.
- Purity: PURPLE purity (WARN_LOW_PURITY shown as is).
- Interpretation: text line from a rule table Nikhil supplies (initially blank / "per
  reporting pathologist"); rules live in one asset file so they can be changed without code.

Steps: (1) read the exact column names on the server (`head -1` of the three files for
26CGH1250 — do not write anchors from memory); (2) `bin/dashboard_builder/parsers/tp53.py`
(stdlib) returning the observation dict; (3) template card on the CNV tab and a one-line
mirror on the Reporting snapshot; (4) `assets/twist_myeloid/tp53_interpretation_rules.tsv`
(empty skeleton, columns: condition, wording); (5) offline check on 26CGH1250 (17p GAIN, no
TP53 variant expected → block renders "no reportable TP53 variant") and on any run8 sample
carrying a TP53 call; (6) nocache re-render of run8, memo line, SOP addendum
(`docs/sops/dashboard_cnv_v2.md`). Dashboard-only: no resume beyond DASHBOARD/REPORT_BUNDLE.

## 2. Then: in-silico dilution of BAF_V2 on 17p (half session)

`tools/patches/2026-09-10/simulate_17p_cnloh.py`: take a diploid-17 run8 sample's allelic
counts (26CGH60), impose cnLOH at 2, 5, 10, 15, 20 % by resampling ALT counts at the sample's
het catalog sites (binomial with p = 0.5 ± f/2, random haplotype sign per site — unphased), keep
depth; run `bin/baf_cnloh_detect.py` (BAF_V2, current parameters) offline on each level, 20
replicates per level; report verdict, f, confidence per level and the false-positive behaviour
on the four clean samples. Output table = the number behind any "sensitivity ~10 %" statement.
Also: measure off-target depth on one run8 BAM (mosdepth outside targets) to decide whether
off-target phasing (memo 18 §3b) is worth an hour later.

## 3. Then, as planned: A3 (site-level assay model) and BAF_V2c

A3: het-only background rebuild (`modules/local/bpt_aggregate_baf.nf`) extended to a site table
with classes (GOOD / WEAK / BIASED / UNSTABLE / UNINFORMATIVE), per-site median, MAD, het
fraction, strand/position bias, and a bias blacklist consumed by BAF_V2. BAF_V2c: beta-binomial
likelihood on counts with per-site φ shrunk towards the panel-wide estimate (48 normals cannot
support free per-site dispersion), CBS-style segmentation on the arm, arm summary kept.

## 4. Decisions for Nikhil (added to v1 §3)

- M parked — confirm; the Twist letter gains the phasing-by-distance table as the density
  argument (common SNP every 3–5 kb on 17p).
- TP53 interpretation rule table: wording and conditions.
- Wet-lab dilution: is a known 17p cnLOH sample (array/WGS-confirmed) available to mix into a
  negative, for the validated floor?
- v1 §3 items unchanged (germline lens parameters, 50–75 % blacklist band, badge count, MNV
  follow-ups, 26CGH1043 8p, FLT3 domain boundaries).

## 5. Order for 10 Sep

1. `git status` clean, memo 18 + this handoff on origin. 2. §1 TP53 block. 3. §2 dilution.
4. Eyeball the 26CGH1043 Genome-wide figure. 5. A1 (containerisation) starts the handout block;
A3/BAF_V2c slot in where the 48-normal re-run makes them natural.
